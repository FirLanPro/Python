| idone.com (Python3) |
repl.it |
Для ПК pycharm |

## Система штрафов:

    print("текст") # вывод в консоль
    print("text :" + str(переменная))

> str  преобразует число в строку

----------------------------------------------------------------------
    if over_speed < 20:
        print("текст")  
после строки с if с условием, переход на следующую строку с отступом 4 пробела или один Tab


    is_town = True

> ### is_town - логическая переменная

> ### elif -объединение else и if

> ### and - объединение условий 
